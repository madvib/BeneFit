# domain-workouts

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build domain-workouts` to build the library.

## Running unit tests

Run `nx test domain-workouts` to execute the unit tests via [Vitest](https://vitest.dev/).
