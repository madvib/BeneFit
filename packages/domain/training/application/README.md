# domain-application

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build domain-application` to build the library.

## Running unit tests

Run `nx test domain-application` to execute the unit tests via [Vitest](https://vitest.dev/).
