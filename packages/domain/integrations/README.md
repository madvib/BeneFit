# domain-integrations

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build domain-integrations` to build the library.

## Running unit tests

Run `nx test domain-integrations` to execute the unit tests via [Vitest](https://vitest.dev/).
