# integrations-domain

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build integrations-domain` to build the library.

## Running unit tests

Run `nx test integrations-domain` to execute the unit tests via [Vitest](https://vitest.dev/).
