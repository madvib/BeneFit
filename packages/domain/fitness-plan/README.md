# domain-fitness-plan

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build domain-fitness-plan` to build the library.

## Running unit tests

Run `nx test domain-fitness-plan` to execute the unit tests via [Vitest](https://vitest.dev/).
