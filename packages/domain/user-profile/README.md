# domain-user-profile

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build domain-user-profile` to build the library.

## Running unit tests

Run `nx test domain-user-profile` to execute the unit tests via [Vitest](https://vitest.dev/).
