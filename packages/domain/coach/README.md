# coach

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build coach` to build the library.

## Running unit tests

Run `nx test coach` to execute the unit tests via [Vitest](https://vitest.dev/).
