# coach-infra

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build coach-infra` to build the library.

## Running unit tests

Run `nx test coach-infra` to execute the unit tests via [Vitest](https://vitest.dev/).
