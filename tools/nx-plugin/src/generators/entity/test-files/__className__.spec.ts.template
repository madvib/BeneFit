import { describe, it, expect } from 'vitest';
import { <%= className %> } from './<%= className %>.js';

describe('<%= className %>', () => {
  describe('create', () => {
    it('should create valid <%= className.toLowerCase() %>', () => {
      const result = <%= className %>.create({
        id: 'test-123',
        // TODO: Add test properties
      });

      expect(result.isSuccess).toBe(true);
      expect(result.value.isActive).toBe(true);
    });

    it('should fail with invalid data', () => {
      // TODO: Add validation tests
    });
  });

  describe('deactivate', () => {
    it('should deactivate <%= className.toLowerCase() %>', () => {
      const <%= className.toLowerCase() %> = <%= className %>.create({
        id: 'test-123',
      }).value;

      <%= className.toLowerCase() %>.deactivate();

      expect(<%= className.toLowerCase() %>.isActive).toBe(false);
    });
  });
});